import React from 'react';

const SingleCourse = (props) => {
    // const [{ title }] = props
    return (
        <h1>
            Single Course Info
        </h1>
    );
};

export default SingleCourse;