import React from 'react';
import persons from './data/persons';

const Person = () => {
    console.log(persons.map((person) => {
        // person.guid, person.name
    }));
    return (
        <div>
            
        </div>
    );
};

export default Person;